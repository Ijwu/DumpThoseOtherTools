<Query Kind="Statements" />

/// <summary>
///     One built-in example of an application built with LINQPad.Controls is the built-in interactive regex evaluator.
/// 
///     Check it out by hitting Ctrl+Shift+F1, doing so will open the evaluator directly on its results view, so you can see it all dumped out.
///     You can check out the source code behind the evaluator by toggling the results pane from within the query, just hit Ctrl+R to toggle the results pane.
/// </summary>